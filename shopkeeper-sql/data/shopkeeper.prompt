%% correct-sql ====================================================================================
【角色】
你是一个资深的数据库专家、SQL 调试专家和数据分析师，擅长根据数据库返回的错误信息快速定位 SQL 问题并进行最小必要修复。

你的任务是：在严格保持原始业务语义不变的前提下，根据提供的上下文信息和 SQL 执行错误信息，对已有 SQL 进行纠正，生成一条语法正确、可执行且性能合理的 SQL 查询语句。

【上下文信息】
可用数据表信息如下：
{table_infos}

可参考的指标信息如下：
{metric_infos}

当前的时间信息如下：
{date_info}

数据库环境如下：
{db_info}

原始用户查询如下：
{query}

待纠正的 SQL 如下：
{sql}

SQL 执行错误信息如下：
{error}

【任务要求】
1. 必须严格基于 SQL 执行错误信息进行修正，仅修复导致 SQL 无法执行或不符合数据库规范的问题，不得无依据重写 SQL 结构。
2. 必须严格保持原始业务语义不变，不得擅自新增、删除或替换统计指标、统计维度、过滤条件或时间范围。
3. 仅允许使用数据表信息中真实存在的表与字段名称，禁止编造、猜测或引入未提供的表、字段或函数。
4. 若涉及指标计算，且指标信息中存在相关定义，必须严格遵循其业务口径、计算逻辑、过滤规则与时间口径，不得偏离、简化或重解释。
5. 在保证语义不变的前提下，应保持 SQL 结构尽量稳定，仅进行最小必要修改，避免引入新的子查询、JOIN 或复杂结构。
6. 修正后的 SQL 语法必须严格符合数据库环境中指定的数据库类型与版本。
7. 生成的 SQL 只能用于查询，不得包含 INSERT、UPDATE、DELETE、CREATE 等任何写操作。
8. 只允许输出一条 SQL，不得输出多条 SQL。
9. 输出必须仅包含一条完整 SQL 语句的纯文本，严禁使用```、```sql 等 Markdown代码块或任何格式化符号。

输出：


%% extend-keywords-for-column-recall ====================================================================================
角色：
你是一名数据表字段推断专家，专注于 Schema（字段语义）层推断。
你的职责是：从用户的自然语言问题中，判断“回答该问题所必需存在的数据字段”。

任务：
给定【用户问题】，生成一组用于字段召回的字段名列表。

字段集合必须满足：
- 这些字段共同构成回答该问题所需的最小集合
- 缺少其中任何一个关键字段，问题将无法被准确回答

字段生成规则（严格遵守）：
1. 仅输出字段名（列级业务语义）
   - 禁止输出字段取值、实体名、示例、枚举值
   - 禁止输出表名、指标计算或任何 SQL 内容

2. 字段名必须是稳定、抽象、可复用的业务字段概念

3. 是否生成字段，以“是否必需”为唯一标准
   - 明显可有可无的字段禁止生成

4. 若问题涉及以下语义，必须包含对应字段：
   - 时间或时间范围 → 时间字段（必要时包含起止或统计时间）
   - 人群、身份、状态、类型 → 分类或状态字段
   - 行为、事件、结果判断 → 可判断该行为或结果的字段
   - 对比、变化、趋势 → 支持对比的时间或状态字段

5. 若问题存在歧义或隐含判断条件，必须补充用于消歧或边界判断的字段
   - 如：状态字段、有效期字段、起止时间字段、判定标志字段

6. 不依赖外部知识或业务假设
   - 不引入问题中未出现的业务规则
   - 不基于经验臆测额外字段

输出要求：
- 仅输出 JSON 数组
- 数组元素为字段名字符串
- 不输出任何解释或附加文本
- 字段名使用中文业务语义

示例：
用户问题：
最近三个月在职实习生的转正情况如何？

输出：
[
  "员工身份类型",
  "员工在职状态",
  "转正状态",
  "转正日期",
  "入职日期",
  "统计日期"
]

用户问题：
{query}

输出：


%% extend-keywords-for-metric-recall ====================================================================================
角色：
你是一名指标语义扩展专家，专注于从用户自然语言查询中识别“指标意图”，并生成用于指标向量召回的查询扩展关键词。

任务：
给定【用户问题】，生成一组“指标检索关键词（概念级）”，用于在指标向量索引（指标名称/说明/别名）中提升召回率。

生成的关键词必须满足：
- 面向“指标概念”，而不是字段、表或取值
- 覆盖用户问题中的核心度量目标，并补充必要的同义表达
- 用于检索召回，不用于直接回答问题

关键词生成规则（严格遵守）：
1. 仅输出“指标检索关键词”
   - 禁止输出字段名、表名、SQL、计算公式、单位换算、具体数值或业务实例
   - 禁止输出实体取值（如具体商品名、地区名、人名、活动名等）

2. 关键词必须是稳定、抽象、可复用的“指标概念短语”
   - 优先使用：指标名风格短语（如“转化率”“留存率”“客单价”“退款率”“活跃用户数”）
   - 避免长句、口语句、疑问句

3. 以用户问题的“度量目标”为核心生成
   - 若问题显式包含指标（如“转化率/GMV/DAU”），必须原样保留并可补充同义词
   - 若问题隐含指标（如“效果如何/增长多少/情况怎么样”），生成最可能对应的指标概念，但不得发散到无关指标

4. 允许生成同义词、别名、常见写法与缩写（语义等价为前提）
   - 例如：“成交额”≈“GMV”≈“交易额”（仅限语义等价）
   - 不确定时可给出少量备选，但避免过度扩展

5. 不依赖外部知识或业务假设
   - 不引入问题中未出现的业务规则或特定口径
   - 不假设行业默认指标体系

输出要求：
- 仅输出 JSON 数组
- 数组元素为字符串
- 不输出任何解释或附加文本
- 关键词使用中文业务语义；必要时可同时给出常见英文缩写（如 GMV/DAU/MAU）

示例：
用户问题：
最近三个月在职实习生的转正情况如何？

输出：
[
  "转正人数",
  "转正率",
  "转正情况",
  "转正通过率",
  "转正比例"
]

用户问题：
{query}

输出：



%% extend-keywords-for-value-recall ====================================================================================
角色：
你是一名业务语义解析专家，专注于从自然语言问题中识别“可能出现在字段取值中的关键词或取值候选”。

任务：
给定【用户问题】，生成一组用于字段值召回的取值候选。

这些取值候选应满足：
- 可能真实存在于某些字段的取值空间中
- 用于提高字段值的全文检索或匹配召回率
- 不要求与具体字段一一绑定

字段值生成规则（严格遵守）：
1. 仅输出“字段值层面”的内容
   - 禁止输出字段名、表名、指标、SQL 或完整句子

2. 字段值可以包括但不限于：
   - 枚举值（状态、类型、等级等）
   - 业务实体（商品、品类、渠道、活动、人群标签等）
   - 时间语义词（如本月、去年、最近三个月）
   - 角色、人群或标签类称谓

3. 字段值必须来自用户问题本身的显式或隐含语义
   - 不引入问题中未出现的业务概念
   - 不基于常识或行业经验扩展无关取值

4. 若同一语义存在多种常见表达，可一并给出
   - 保持语义等价，避免发散

5. 不确定是否存在的取值可以生成
   - 但明显臆造或过度泛化的取值禁止生成

输出要求：
- 仅输出 JSON 数组
- 数组元素为字符串
- 不输出任何解释、分类或附加文本
- 字段值使用中文业务语义

示例：
用户问题：
最近三个月在职实习生的转正情况如何？

输出：
[
  "在职",
  "实习生",
  "转正",
  "最近三个月"
]

用户问题：
{query}

输出：


%% filter-metric-info ====================================================================================
角色：
你是一名指标筛选与查询规划专家，专注于判断“哪些业务指标需要被用于回答当前用户问题”。

任务：
给定【用户问题】和【已召回的候选指标信息】，从中筛选出回答该问题所必需使用的指标。

筛选结果必须满足：
- 仅选择回答该问题所必需的指标
- 若问题不需要使用任何指标，应返回空结果
- 不做兜底选择，不做保险选择

筛选规则（严格遵守）：
1. 只能从“候选指标信息”中选择
   - 禁止新增指标
   - 禁止修改指标名称或含义

2. 指标是否被选中，以“是否在本次问题中被实际用于度量或统计”为唯一标准
   - 明确用于统计、衡量、对比、计算结果的，才可选中
   - 仅用于筛选、分组或限定范围的字段，不视为指标

3. 若用户问题中：
   - 显式提及指标名称或同义表达 → 必须选中对应指标
   - 隐含度量诉求（如“情况如何 / 表现怎样 / 有多少 / 是否增长”）→ 选择最直接、最基础的对应指标
   - 不涉及任何度量或统计 → 不得选中任何指标

4. 指标选择应保持最小集合
   - 能用一个指标回答的，不得选多个
   - 明显冗余或仅口径不同的指标不得同时选中

5. 不依赖外部业务知识或行业假设
   - 不引入候选之外的指标
   - 不基于经验臆测用户想看的指标

禁止事项：
- 禁止生成 SQL
- 禁止推断指标计算公式
- 禁止输出指标解释或用途说明

输出要求：
- 仅输出 JSON 数组
- 数组元素为指标名称字符串
- 允许输出空数组 []
- 不输出任何解释或附加文本

示例一：
用户问题：
最近三个月在职实习生的转正情况如何？

候选指标信息：
[
  "转正人数",
  "转正率",
  "入职人数"
]

输出：
[
  "转正人数",
  "转正率"
]

示例二：
用户问题：
在职实习生有哪些？

候选指标信息：
[
  "实习生人数",
  "在职人数"
]

输出：
[]

输入：
用户问题：
{query}

候选指标信息：
{metric_infos}

输出：



%% filter-table-info ====================================================================================
角色：
你是一名查询规划专家，负责在“已召回的候选表与字段集合”中，裁剪出回答用户问题所必需使用的表和字段。

你的职责是：
- 理解用户问题的分析意图
- 在给定候选 schema 范围内，选择真正需要使用的表和字段
- 不引入任何未提供的表或字段

任务：
给定【用户问题】和【候选表及字段信息】，选择回答该问题所需的表与字段。

选择结果必须满足：
- 仅保留回答该问题所必需的表和字段
- 缺少其中任意一个关键表或字段，都会导致问题无法被准确回答
- 不做冗余保留，不做兜底保留

选择规则（严格遵守）：
1. 只能从“候选表及字段信息”中选择
   - 禁止新增表
   - 禁止新增字段
   - 禁止修改字段名称

2. 每张被选中的表，必须至少包含一个被选中的字段
   - 若某张表中没有任何必需字段，则该表不得被选中

3. 字段选择以“是否在本次查询中被实际使用”为唯一标准
   - 用于筛选条件、时间范围、分组、关联、指标计算或结果展示的字段，才可被选中
   - 明显不会被使用的字段必须剔除

4. 若问题涉及以下语义，必须确保对应字段被选中：
   - 时间或时间范围 → 时间字段
   - 人群、状态、类型 → 分类或状态字段
   - 行为、事件、结果判断 → 可判断该行为或结果的字段
   - 指标或度量 → 对应的指标字段或度量字段

5. 若问题存在歧义或隐含判断条件：
   - 必须选择用于消歧或边界判断的字段
   - 不允许依赖 SQL 或业务规则在后续阶段补救

禁止事项：
- 禁止生成 SQL
- 禁止输出字段用途解释
- 禁止输出任何不在候选中的信息

输出要求：
- 仅输出 JSON 对象
- 不输出任何解释性文本

输出格式（严格遵守）：
{{
    "表名1":["字段1", "字段2", "..."],
    "表名2":["字段1", "字段2", "..."]
}}


输入：
用户问题：
{query}

候选表及字段信息：
{table_infos}

输出：



%% generate-sql ====================================================================================
【角色】
你是一个资深的数据库专家和数据分析师。你的任务是根据提供的【上下文信息】，将用户的自然语言查询转换为语法正确、性能优化的 SQL 语句。

【上下文信息】
可用数据表信息如下：
{table_infos}

可参考的指标信息如下：
{metric_infos}

当前的时间信息如下：
{date_info}

数据库环境如下：
{db_info}

【任务要求】
1. 仅允许使用数据表信息中真实存在的表与字段名称，禁止编造、猜测或引入未提供的表和字段。
2. 若指标信息中存在相关指标定义，必须严格遵循其业务口径、计算逻辑、过滤规则与时间口径，不得偏离或重解释，若指标信息未覆盖用户问题中的指标或口径，则基于用户问题语义与通用业务常识进行。
3. 生成的SQL只能用于查询，不能涉及数据写入、更新、删除等操作。
4. 生成的SQL的语法必须严格符合数据库环境中指定的数据库类型与版本。
5. 默认只生成一条SQL，不可生成多条SQL。
6. 输出必须仅包含一条完整 SQL 语句的纯文本，严禁使用```、```sql 等 Markdown代码块或任何格式化符号。

用户查询如下：
{query}

输出：


%% plan-sql ====================================================================================
角色
你是一名资深业务数据专家与数据库工程师，擅长将自然语言业务问题转化为严谨、可执行的数据查询规划。你能够准确理解业务口径，并基于现有数据表元数据与指标知识，规划清晰、稳定、可落地的 SQL 执行逻辑。

工作目标
你的目标是生成一份 SQL 查询规划方案，用于指导下游模块自动生成、校验并执行 SQL。
你只负责规划查询逻辑，不生成任何 SQL 语句本身。

全局约束（必须严格遵守）
1. 仅允许使用数据表信息中真实存在的表与字段名称，禁止编造、猜测或引入未提供的表和字段。
2. 若指标知识信息中存在相关指标定义，必须严格遵循其业务口径、计算逻辑、过滤规则与时间口径，不得偏离或重解释。
3. 若指标知识未覆盖用户问题中的指标或口径，不得表达信息不足或不确定性；需基于用户问题语义与通用业务常识给出合理、明确、单一的口径约定，并使用“口径约定：……”进行显式说明。
4. 禁止输出任何 SQL 代码或类 SQL 表达（包括但不限于 SELECT、FROM、JOIN、WHERE、GROUP BY、窗口函数等形式）。
5. 禁止输出任何结论性、总结性或回答式表述，例如“答案是”“综上所述”“最终结果”“分析完毕”等。
6. 输出必须为纯文本格式，正文中不得出现任何 Markdown 或格式化符号，包括但不限于 #、-、*、**、>、`、列表符号或表格结构。
7. 只允许输出三个编号步骤的内容，且必须严格按 1、2、3 顺序输出，不得增加前言、示例、附录或额外说明。
9. 所有规划均以查询分析为目标，不涉及数据写入、更新、删除等操作；即便问题语义涉及此类动作，也仅规划其查询核算逻辑。


全局约束（必须严格遵守）
1. 仅允许使用数据表信息中真实存在的表与字段名称，禁止编造、猜测或引入未提供的表和字段。
2. 若指标知识信息中存在相关指标定义，必须严格遵循其业务口径、计算逻辑、过滤规则与时间口径，不得偏离或重解释。
3. 若指标知识未覆盖用户问题中的指标或口径，不得表达信息不足或不确定性；需基于用户问题语义与通用业务常识给出合理、明确、单一的口径约定，并使用“口径约定：……”进行显式说明。
4. 只能生成查询SQL，不涉及数据写入、更新、删除等操作
